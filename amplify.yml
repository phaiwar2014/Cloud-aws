version: 1
frontend:
  phases:
    # เราข้าม preBuild ไปได้เลยถ้าไม่มีการลง Library เพิ่มเติม
    build:
      commands:
        - echo "Building my car service app..."
  artifacts:
    baseDirectory: .
    files:
      - '**/*'
  cache:
    paths: [] # ปล่อยว่างไว้ถ้าไม่ได้ใช้ node_modules